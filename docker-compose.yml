version: '3.8'

services:
  web:
    build: .
    container_name: secure_coding_app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - ./data:/app/data
    restart: unless-stopped

